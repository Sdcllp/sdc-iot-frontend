// import React from "react";

// function RoomList({ floor, setRoom }) {
//   const rooms = Array.from({ length: 10 }, (_, i) => 101 + i);

//   return (
//     <div style={{ padding: "20px" }}>
//       <h3>Floor {floor} Rooms</h3>

//       {rooms.map((room) => (
//         <button key={room} onClick={() => setRoom(room)} style={{ margin: 5 }}>
//           Room {room}
//         </button>
//       ))}
//     </div>
//   );
// }
// // 
// export default RoomList;